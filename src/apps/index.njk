---
title: "アプリ一覧"
layout: base.njk
---

<h1>アプリ一覧</h1>
<ul>
  {%- for app in collections.apps %}
  <li>
    <h3><a href="{{ app.url }}">{{ app.data.title or (app.url | replace("/apps/", "") | replace("/", "")) }}</a></h3>
    {% if app.data.description %}
      <p>{{ app.data.description }}</p>
    {% endif %}
  </li>
  {%- endfor %}
</ul>
